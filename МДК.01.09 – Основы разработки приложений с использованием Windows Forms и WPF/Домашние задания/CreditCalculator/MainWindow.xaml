<Window x:Class="CreditCalculator.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Кредитный калькулятор" Height="450" Width="500" ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="Сумма кредита:" VerticalAlignment="Center" FontSize="14"/>
        <TextBox x:Name="tbAmount" Grid.Row="0" Grid.Column="1" Margin="0,5" FontSize="14" Text="1000000"/>

        <TextBlock Grid.Row="1" Grid.Column="0" Text="Процентная ставка (%):" VerticalAlignment="Center" FontSize="14"/>
        <TextBox x:Name="tbRate" Grid.Row="1" Grid.Column="1" Margin="0,5" FontSize="14" Text="12"/>

        <TextBlock Grid.Row="2" Grid.Column="0" Text="Срок (месяцев):" VerticalAlignment="Center" FontSize="14"/>
        <TextBox x:Name="tbTerm" Grid.Row="2" Grid.Column="1" Margin="0,5" FontSize="14" Text="60"/>

        <Button x:Name="btnCalculate" Grid.Row="3" Grid.Column="1" Content="Рассчитать" 
                FontSize="16" Margin="0,10" Padding="10,5" HorizontalAlignment="Left"
                Click="btnCalculate_Click"/>

        <GroupBox Grid.Row="4" Grid.ColumnSpan="2" Header="Результат" FontSize="14" Margin="0,10">
            <StackPanel Margin="10">
                <TextBlock x:Name="tbMonthlyPayment" FontSize="16" Margin="0,3"/>
                <TextBlock x:Name="tbTotalPayment" FontSize="16" Margin="0,3"/>
                <TextBlock x:Name="tbOverpayment" FontSize="16" Margin="0,3"/>
            </StackPanel>
        </GroupBox>

        <Button x:Name="btnClear" Grid.Row="5" Grid.Column="1" Content="Очистить" 
                FontSize="14" Padding="10,5" HorizontalAlignment="Left"
                Click="btnClear_Click"/>

    </Grid>
</Window>